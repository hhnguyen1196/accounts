server:
  servlet:
    context-path: /api

spring:
  application:
    name: accounts
  datasource:
    url: jdbc:mysql://localhost:3306/platform-dev
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    defer-datasource-initialization: true

  sql:
    init:
      mode: always
